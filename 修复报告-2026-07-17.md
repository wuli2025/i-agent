# i-agent 兼容性修复报告（2026-07-17）

依据 `kimi-k3-bench-2026-07-17/评测报告.html`：i-agent 泳道数据作废的直接原因是
**看到环境里的 `MINIMAX_API_KEY`（本是给生图工具用的）就把对话模型劫持到 MiniMax，
无视显式设置的 `ANTHROPIC_BASE_URL`，且全程无任何提示**；harness 注释还记录了第二个坑：
**HTTP 客户端认 `http_proxy` 但不认 `no_proxy`**，发往 127.0.0.1 计量代理的请求被塞进
系统代理（clash）直接 502。以下四处修复全部落在 `源码/src`，已编译通过并端到端验证。

## 修了哪里

### 1. 供应商选择：显式配置优先且排他（config.rs）
根因：所有供应商（显式配置的 + 靠扫描密钥环境变量入列的内置项）混在同一条候选链里。
评测中 anthropic 排第一，但第一个请求 403（Kimi 额度耗尽）后，`chat()` 沿链静默回退
到了 minimax——模型换了、计量代理绕过了，表面一切正常。

改动：候选分两层。显式层 = `I_AGENT_*` 环境变量 / `ANTHROPIC_*` 环境变量 / 配置文件
providers；内置层 = 靠 `DEEPSEEK_API_KEY`、`MINIMAX_API_KEY` 等密钥扫描入列的内置供应商。
**只要显式层非空，内置层就不进自动候选链，也不做回退兜底。**
- `--provider` 点名时内置供应商仍可选中（用户明说了就算数），不影响老用法；
- 没有任何显式配置时，内置扫描行为与原来完全一致（已验证不误伤）。

### 2. 代理：按目标 URL 认 no_proxy，loopback 永远直连（llm.rs）
根因：`proxy_from_env()` 只读 `HTTP(S)_PROXY`/`ALL_PROXY`，完全不看 `no_proxy`，
而且代理是挂在全局 Agent 上的，同一实例打所有目标。

改动：改为 `proxy_for(url)` / `agent_http_for(url)`，按目标主机逐个判定：
- 解析 `no_proxy`/`NO_PROXY`（逗号分隔），支持三种写法：`*` 全匹配、`127.*` 这类
  glob（clash 系代理的常见写法，curl/reqwest 都不认，这次专门支持）、普通域名按
  curl 语义做后缀匹配（`example.com` 同时命中自身与子域），可带 `:端口`；
- **localhost / 127.x / ::1 无条件直连**——把发往本机的请求塞给外部代理没有任何
  正确场景，代理无法回环路由只会 502；
- 三个 HTTP 调用点全部切换：LLM 对话（llm.rs）、生图 API 与图片下载（tools/image.rs，
  API 端点和返回的 CDN 域名现在分别判定）、网页抓取（tools/web.rs）。

### 3. 供应商回退不再静默（llm.rs `chat()`）
回退意味着换模型（可能连协议/计费方都换了）。现在每次从失败供应商切到下一个时向
stderr 打警告：`[警告] 供应商 X 调用失败，回退到 Y（模型 A → B）`，quiet 模式也打。

### 4. 无头模式启动亮明身份（main.rs）
`-p` 模式启动时打印 `[i-agent] 供应商 X | 模型 Y | 端点 Z`（`-q` 静默）。评测里
"跑错模型还一路显得很正常"的另一半原因就是没有任何可见线索；现在配置错了日志里
一眼可见。同时更新了 `--help` 与 README：补充 ANTHROPIC_* 用法、供应商优先级规则、
代理行为说明，缺密钥时的报错也列全了 ANTHROPIC_*/MINIMAX_API_KEY。

## 验证

- `cargo test`：8 通过（含新增 4 个代理匹配单元测试：URL 主机提取、glob、
  no_proxy 列表匹配、loopback 兜底）；`cargo build --release` 通过。
- 端到端（本地 mock Anthropic SSE 服务，复刻评测环境）：
  - **场景 A（评测原故障）**：`MINIMAX_API_KEY` 干扰项 + `ANTHROPIC_BASE_URL=127.0.0.1`
    + 系统代理指向不通端口 → 正确选中 anthropic、直连 loopback、任务完成，exit 0；
  - **场景 B（403 额度耗尽）**：当场失败并透传 `HTTP 403: usage limit`，exit 1，
    0.4s 结束，未碰 MiniMax；
  - **场景 C（回归）**：无显式配置、仅 `MINIMAX_API_KEY` → 内置扫描仍选中 minimax。

## 追加：国产供应商一等公民支持（同日第二批改动）

内置供应商表全面升级（config.rs `builtin_providers()`），能走 Anthropic 协议的
一律切到各家官方 Anthropic 端点（思考内容由服务端剥离，不混 `<think>`，长任务更稳）；
端点与模型名以 2026-07 评测 harness 实测为准：

| 名字 | 密钥变量 | 默认模型 | 端点 / 协议 |
|---|---|---|---|
| kimi（新） | `KIMI_API_KEY` | k3 | api.kimi.com/coding · Anthropic |
| deepseek | `DEEPSEEK_API_KEY` | deepseek-chat | api.deepseek.com/anthropic · Anthropic |
| glm | `ZHIPU_API_KEY` / `GLM_API_KEY` | glm-4.6 | open.bigmodel.cn/api/anthropic · Anthropic |
| minimax | `MINIMAX_API_KEY` | MiniMax-M3（原 M2.7/v1） | api.minimaxi.com/anthropic · Anthropic |
| moonshot（原 kimi 改名） | `MOONSHOT_API_KEY` | kimi-k2-0905-preview | api.moonshot.cn/v1 · OpenAI 兼容 |
| qwen | `DASHSCOPE_API_KEY` / `QWEN_API_KEY` | qwen-plus | dashscope · OpenAI 兼容 |
| siliconflow | `SILICONFLOW_API_KEY` | DeepSeek-V3 | api.siliconflow.cn/v1 · OpenAI 兼容 |

- 内置表支持每家多个密钥变量别名 + 显式协议声明；
- 老 `kimi`（月之暗面开放平台）改名 `moonshot` 保留——它与 kimi.com 的密钥不通用；
- README / --help / 缺密钥报错 / package.ps1（顺带修正了它引用 README、Dockerfile、
  config.example.json 的断路径，示例配置改为直接拷贝仓库根的 config.example.json）同步更新。

验证：
- 五家端点假密钥探测全部返回 401 鉴权错（而非 404 路径错），URL 拼接正确；
- 真实密钥端到端：**Kimi K3**（api.kimi.com/coding，只设 `KIMI_API_KEY`）与
  **MiniMax M3**（api.minimaxi.com/anthropic）均正常完成对话并上报用量（含缓存命中）。

## 其他

- 构建脚本（`gen_embedded.ps1`/`package.ps1`）期望 assets 在 `源码/assets`，当前磁盘上
  在上一层，已复制过去使构建可用。
- harness 侧的临时规避（`MINIMAX_KEY` 改名 + 摘代理变量）在本修复合入后不再必要，
  重跑评测可直接用标准变量名。Linux 泳道二进制（`bin/i-agent-linux`）需在 WSL 里
  重新编译后替换。
